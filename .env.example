# Copy to .env (docker compose reads it automatically) and fill in real values.

# Telegram bot token — used by PocketBase's auth-with-telegram endpoint.
# Get it from @BotFather. Keep the real token out of version control.
BOT_TOKEN=869......:AAFvs.........
NEXT_PUBLIC_POCKETBASE_URL=
